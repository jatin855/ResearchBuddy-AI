# ResearchBuddy Backend

Spring Boot API that stores paper metadata in PostgreSQL and delegates extraction, retrieval, summaries, QA, and comparison to the FastAPI AI service.

Run locally:

```bash
mvn spring-boot:run
```
